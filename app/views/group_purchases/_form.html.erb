<%= form_with(model: group_purchase) do |form| %>
  <% if group_purchase.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group_purchase.errors.count, "error") %> prohibited this group_purchase from being saved:</h2>

      <ul>
        <% group_purchase.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :purchase_id, style: "display: block" %>
    <%= form.text_field :purchase_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
